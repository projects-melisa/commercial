node_modules/
.next/
out/
dist/
build/
.cache/
.env*
!.env.example
.vite/
next-env.d.ts
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
.DS_Store

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Database snapshots taken before a destructive import. They contain real email
# addresses, profile names and notification bodies, so they stay out of the repo.
backups/

# Per-developer Claude Code settings. `launch.json` is shared; this one is not.
.claude/settings.local.json
